from .sql import SQL
example = SQL(host="localhost",user="bresli",passwd="bresli",database="bresli")

#for tbl, rows in sql.tbl_data.items():
#...     print(tbl,[k for k,v in rows.items() if v["Key"]=="PRI"])

